# Client templates

Templates used when generating or scaffolding Fiducia clients. They must stay
aligned with `PROTOCOL.md` and the generated-interface contract. Regenerate the
affected clients and run their contract tests after changing a template.
