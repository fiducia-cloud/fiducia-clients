{
  "name": "@fiducia/client",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "license": "UNLICENSED",
  "description": "Fiducia HTTP client (TypeScript). Zero runtime deps; types from @fiducia/interfaces.",
  "main": "fiducia.ts",
  "types": "fiducia.ts",
  "scripts": {
    "test:contract": "python3 ../../contract/bin/check_surface.py --lang ts",
    "pretest": "python3 ../../contract/bin/check_surface.py --lang ts",
    "prepublishOnly": "npm run lint:ores && python3 ../../contract/bin/check_surface.py --prepublish --lang ts",
    "test": "node --test --experimental-strip-types fiducia.test.ts default-fetch-binding.test.ts locking.test.ts sync.test.ts secrets.test.ts && node --check --experimental-strip-types locking.ts",
    "test:browser": "tsc -p tsconfig.browser.json && node browser-smoke.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "lint:ores": "if [ -f ../../.ores-lint/lint.sh ]; then sh ../../.ores-lint/lint.sh; else echo 'ores-lint not installed'; fi"
  },
  "files": [
    "fiducia.ts",
    "locking.ts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fiducia-cloud/fiducia-clients.git",
    "directory": "clients/ts"
  },
  "dependencies": {
    "@fiducia/interfaces": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  }
}
