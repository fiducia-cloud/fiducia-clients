{
  "name": "@fiducia/client",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "license": "UNLICENSED",
  "description": "Fiducia HTTP client (TypeScript). Zero runtime deps; types from @fiducia/interfaces.",
  "main": "fiducia.ts",
  "types": "fiducia.ts",
  "scripts": {
    "test": "node --test --experimental-strip-types fiducia.test.ts default-fetch-binding.test.ts locking.test.ts sync.test.ts secrets.test.ts service-discovery.test.ts && node --check --experimental-strip-types locking.ts && node --check --experimental-strip-types service-discovery.ts",
    "test:browser": "tsc -p tsconfig.browser.json && node browser-smoke.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "files": [
    "fiducia.ts",
    "locking.ts",
    "service-discovery.ts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fiducia-cloud/fiducia-clients.git",
    "directory": "clients/ts"
  },
  "dependencies": {
    "@fiducia/interfaces": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
