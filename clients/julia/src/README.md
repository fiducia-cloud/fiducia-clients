# Fiducia client (Julia) — source

Module source for the Julia Fiducia client. `Fiducia.jl` defines `module Fiducia`: a thin HTTP client built on HTTP.jl and JSON.jl that implements the shared `PROTOCOL.md` contract. Package manifest (`Project.toml`) and the public README live one level up in `clients/julia`.
