# Package

version       = "0.1.0"
author        = "fiducia.cloud"
description   = "Fiducia HTTP client — distributed locks, semaphores, idempotency, config KV, rate limiting, cron, leader election, and service discovery."
license       = "Proprietary"
srcDir        = "src"

# Dependencies

requires "nim >= 1.6"
