# Fiducia client (Kotlin) — source

Package `cloud.fiducia`. `Fiducia.kt` is the client source: a thin HTTP client over `java.net.http.HttpClient` with kotlinx-serialization-json bodies (returns `JsonElement`), implementing the shared `PROTOCOL.md` contract. Gradle build files and the public README live at the `clients/kotlin` root.
