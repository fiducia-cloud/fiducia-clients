# sbt build configuration

sbt's project directory. `build.properties` pins the sbt launcher version used
to build and cross-publish the Scala Fiducia client; the build definition itself
lives in `../build.sbt`.
