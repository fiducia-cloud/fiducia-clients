# .github

GitHub-specific repository configuration.

- `dependabot.yml` — weekly dependency-update checks for the TypeScript client's npm packages and the repo's GitHub Actions.
- `workflows/` — CI and audit pipelines (see `workflows/README.md`).
