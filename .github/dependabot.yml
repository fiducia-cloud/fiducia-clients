# Dependabot config: weekly update checks for the TypeScript client's npm deps and GitHub Actions.
version: 2
updates:
  # The only package.json lives in the TypeScript client.
  - package-ecosystem: npm
    directory: "/clients/ts"
    schedule:
      interval: weekly
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
