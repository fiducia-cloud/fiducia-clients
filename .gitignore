# Rust
target/
Cargo.lock

# wasm-pack output (clients/rust-wasm/pkg)
pkg/

# Node / TypeScript
node_modules/
clients/*/.gradle/
dist/
*.tsbuildinfo

# Python
__pycache__/
*.pyc
.venv/

# Go
bin/
vendor/

# Java / C#
*.class
bin/
obj/

# Dart
.dart_tool/
.packages
clients/dart/pubspec.lock

# Elixir
_build/
deps/
*.beam

# misc
# --- environment / secrets (sops + age) -------------------------------------
# Only env/enc/*.env.enc is tracked. Every plaintext form stays out of git;
# `just env-check` enforces it. See env/README.md.
# (`*.env` already matches at any depth — `**/*.env` is stated for clarity.)
.env
.env.*
*.env
**/*.env
env/dec/
!.env.example

# age private keys — never commit these.
*.agekey
*.age-key
keys.txt
sops-private*.txt

*.log
.DS_Store

# Scratch / git worktrees (tmp/worktrees/*)
tmp/

# Gleam
build/

# Swift (SwiftPM)
.build/
.swiftpm/

# Zig
.zig-cache/
zig-out/

# Haskell (Cabal / Stack)
dist-newstyle/
.stack-work/

# Nim
nimcache/

# --- hardened baseline (auto) ---
Thumbs.db
*.swp
*~
.direnv/
**/.gradle/
.zed/
.zed-pack/
tmp
temp
tmp/worktrees
temp/worktrees
